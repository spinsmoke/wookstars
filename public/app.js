const uploadBtn = document.getElementById("uploadBtn");
const status = document.getElementById("status");

uploadBtn.addEventListener("click", () => {
  status.textContent =
    "Uploads coming soon 👀 Anonymous mode enabled.";
});
